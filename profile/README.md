# Profile

Profiling utilities for applications.

## API Surface

- `Duration(start, name)` logs elapsed wall-clock time
- `MemUsage()` prints heap allocation statistics
- `CpuUsagePercent()` prints process CPU usage information

## Example

See `profile/test/main.go` for a minimal executable example.
