# Common Question Prompts

Used on the command line.

## API Surface

- `Question(question string) string`
- `QuestionOpts(question string, options []string) string`
- `QuestionInt(question string) int`
- `Confirm(question string) bool`

## Notes

This package is intentionally minimal and fits well with `imnu` and other CLI-oriented modules in the repository.
