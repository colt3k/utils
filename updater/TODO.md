
# Future Features

# Grouping
- add grouping so only those are checked in a group
  - if one of a group is found the skip the other group, handy when used in multiple environments

- if two in group are good then skip rest
  - set a goal of X out of Y found then ok to skip the rest

